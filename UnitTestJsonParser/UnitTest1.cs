using System;
using System.Collections.Generic;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using SoftEngChat;
namespace UnitTestJsonParser
{
    [TestClass]
    public class UnitTest1
    {/*
        List<User> userList = new List<User>();
        static String fileName = "NUit.txt";
        static string filePath = AppDomain.CurrentDomain.BaseDirectory + @"\" + fileName;
        DatabaseManegement db = new DatabaseManegement();
        [TestInitialize()]
        public void Initialize()
        {
            userList.Add(new User("Anders", "mail", "112"));
            userList.Add(new User("Robin", "mail", "911"));
            
            db.DBwrite(userList, filePath);
            
        }


        [TestMethod]
        public void readFromFile()
        {
            userList = db.DBread(filePath);
            Assert.IsNotNull(userList);
        }*/
    }
}
